# Editor / app data
.freebuff/
.continue/

# Windows artifacts
/nul
Thumbs.db
.DS_Store

# Node modules (reinstall with npm install)
node_modules/
package-lock.json

# Files >50MB (GitHub limit - upload these to server directly)
assets/video4.mp4
assets/videofotter.gif

# Environment & secrets (NEVER commit real .env)
.env
.env.local
*.log
uploads/packages/*
!uploads/packages/.gitkeep
