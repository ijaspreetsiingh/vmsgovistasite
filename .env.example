# Copy this file to .env on your server and fill in real values
# NEVER commit .env to git!

# Database
DB_HOST=localhost
DB_USER=vmsgovis1_admin
DB_PASS=Ijaspreet@121#..
DB_NAME=vmsgovis1_vms
DB_CHARSET=utf8mb4

# SSL for DB (production)
DB_SSL_CA=/etc/ssl/certs/ca-certificates.crt

# Settings encryption key (generate once: openssl rand -base64 32)
SETTINGS_ENCRYPTION_KEY=generate_your_own_32_byte_base64_key

# SMTP certs
SMTP_CA_FILE=/etc/ssl/certs/ca-certificates.crt